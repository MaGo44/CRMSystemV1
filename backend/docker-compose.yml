services:
  postgres:
    image: postgres:15-alpine
    container_name: review-crm-db
    environment:
      POSTGRES_DB: reviewcrm
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: